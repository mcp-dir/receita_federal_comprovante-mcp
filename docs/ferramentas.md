# Ferramentas

Receita Federal: Comprovante de Pagamento expõe 1 ferramenta (todas somente leitura).

### 1. `receita_federal_comprovante_consultar`
**Input**: `data_inicio` (opcional), `data_fim` (opcional), `documento_numero` (opcional), `codigo_receita` (opcional), `perfil_procurador_cnpj` (opcional), `perfil_procurador_cpf` (opcional), `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional)

Receita Federal: Comprovante de Pagamento, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Receita Federal: Comprovante de Pagamento
```
